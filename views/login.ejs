<!DOCTYPE html>
<html lang="en">

<head>
  <link rel='stylesheet' href='/stylesheets/login.css' />
  <% include ./partials/head %>
</head>

<body>
  <% include ./partials/nav %>

  <div class="container">
    <div class="card card-primary">
      <div class="card-body">
        <div class="card-header">
          Welcome sign in
        </div>
        <div class="card-text">
          <form class="create-form" action="/auth/login" method="post">
            <br>
            <div class="form-group row mb-2">
              <div class="col col-md-4">
                <div class="row">
                  <label class="col-form-label" for="username">
                    Username
                  </label>
                </div>
                <div class="row">
                  <input type="text" id="username" name="username" class="form-control"
                    <% if(typeof user !== 'undefined') {%> value="<%= user.username %>" <% } %>
                    placeholder="Please Enter Username" required="">
                  <% if(typeof usernameErr !== 'undefined') {%>
                  <span class="err"><%= usernameErr.msg %> </span>
                  <% } %>
                </div>
              </div>
            </div>

            <div class="form-group row mb-2">
              <div class="col col-md-4">
                <div class="row">
                  <label class="col-form-label" for="password">
                    Password
                  </label>
                </div>
                <div class="row">
                  <input type="password" id="password" name="password" class="form-control"
                    <% if(typeof user !== 'undefined') {%> value="<%= user.password %>" <% } %>
                    placeholder="Please Enter Password" required="">
                  <% if(typeof passwordErr !== 'undefined') {%>
                  <span class="err"><%= passwordErr.msg %> </span>
                  <% } %>
                </div>
              </div>
            </div>
            <p>Don't have an account? <a href="/auth/register">Signup</a> ,it's free.</p> 

            <div class="form-group row mb-2">
              <div class="col col-md-4">
                <div class="row">
                  <br><br>
                </div>
                <div class="row" style="justify-content: flex-end;">
                  <button class="btn btn-sm btn-primary" type="submit">Sign In</button>
                </div>
              </div>
            </div>

            <br>
          </form>
        </div>
      </div>
    </div>
  </div>

</body>
<% include ./partials/footer %>

</html>